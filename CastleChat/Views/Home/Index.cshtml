@model IEnumerable<CastleChat.Models.Castle>
@{
    ViewData["Title"] = "Home";
}

<div class="text-center">
    <h1>Forums</h1>
</div>

@foreach (var i in Model)
{
<div class="alert alert-secondary border-0 rounded-0 text-black">
    <a href="/Room/@i.id"><strong>@i.title</strong></a>

</div>
}